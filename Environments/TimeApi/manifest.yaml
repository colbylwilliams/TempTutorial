name: TimeApi
version: 1.0.0
summary: Azure Function API
description: Deploys an Azure Function App, Storage Account, and Application Insights
runner: ARM
templatePath: azuredeploy.json

parameters:
  - id: name
    name: Name
    type: string
    required: true
  - id: supportsHttpsTrafficOnly
    name: 'Supports Https Traffic Only'
    type: boolean
    required: true
